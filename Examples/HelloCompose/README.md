# HelloCompose

A two-service example demonstrating docker-compose support on WendyOS.

- **api** — a minimal HTTP server on port 8080 (host networking)
- **client** — polls the API, prints the response, and exits

## Run

```sh
wendy run
```
