// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v7.34.0
// source: wendy/agent/services/v2/robot_service.proto

package agentpbv2

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	WendyRobotService_LoadUnit_FullMethodName             = "/wendy.agent.services.v2.WendyRobotService/LoadUnit"
	WendyRobotService_PutRecord_FullMethodName            = "/wendy.agent.services.v2.WendyRobotService/PutRecord"
	WendyRobotService_ClearRecord_FullMethodName          = "/wendy.agent.services.v2.WendyRobotService/ClearRecord"
	WendyRobotService_PutSession_FullMethodName           = "/wendy.agent.services.v2.WendyRobotService/PutSession"
	WendyRobotService_ClearSession_FullMethodName         = "/wendy.agent.services.v2.WendyRobotService/ClearSession"
	WendyRobotService_SetProfileKind_FullMethodName       = "/wendy.agent.services.v2.WendyRobotService/SetProfileKind"
	WendyRobotService_SetStableId_FullMethodName          = "/wendy.agent.services.v2.WendyRobotService/SetStableId"
	WendyRobotService_StreamJointPositions_FullMethodName = "/wendy.agent.services.v2.WendyRobotService/StreamJointPositions"
	WendyRobotService_DescribeStore_FullMethodName        = "/wendy.agent.services.v2.WendyRobotService/DescribeStore"
)

// WendyRobotServiceClient is the client API for WendyRobotService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// WendyRobotService reaches the device's robot calibration store from off the
// device.
//
// A calibration is a fact about the machine, so it lives on the machine — one
// JSON file per unit under /var/lib/wendy/robot, beside the camera registries
// the agent already owns. Without this service the calibration wizard can only
// run from inside an admin-entitled container on the robot itself, because the
// alternative — writing the record into the operator's home directory — turns a
// per-robot fact into a per-laptop one and the robot silently stops knowing its
// own calibration the moment somebody else connects to it.
//
// The surface is deliberately a transport for one Go interface,
// internal/shared/robotcal.Store, and nothing else: one RPC per method, no
// conveniences. The agent serves it from the same robotcal.FileStore the
// on-device path opens directly, so the two cannot drift apart — there is one
// implementation of the store's semantics, reached two ways.
//
// What the platform does NOT do here is interpret a calibration. `payload` and
// a session's per-step results are opaque bytes end to end: a rigid transform
// for a humanoid's camera, per-joint zeros and travel ranges for a 6-DOF arm,
// and something neither anticipates for the next robot. Only the universal part
// — did it pass, by how much, when, and by what method — is modelled.
type WendyRobotServiceClient interface {
	// LoadUnit returns everything one physical robot knows about itself. A unit
	// nobody has ever calibrated is not an error: it comes back zero-valued, with
	// its name filled in.
	LoadUnit(ctx context.Context, in *LoadUnitRequest, opts ...grpc.CallOption) (*LoadUnitResponse, error)
	// PutRecord stores one calibration result, whatever its verdict. A failed
	// calibration is recorded too — "measured and it does not fit" is a fact
	// worth keeping, and it is not the same as never having run.
	PutRecord(ctx context.Context, in *PutRecordRequest, opts ...grpc.CallOption) (*PutRecordResponse, error)
	// ClearRecord invalidates a calibration, and any half-finished attempt at it.
	// After a hand is replaced the old calibration is worse than none, because
	// everything downstream still trusts it. Clearing an id the unit does not
	// have is NOT_FOUND, so a typo cannot read as success.
	ClearRecord(ctx context.Context, in *ClearRecordRequest, opts ...grpc.CallOption) (*ClearRecordResponse, error)
	// PutSession checkpoints an in-progress procedure. A seven-joint sweep on a
	// limp humanoid needs two people and a gantry; making them start over because
	// a terminal was closed at joint four is how a procedure stops being run.
	PutSession(ctx context.Context, in *PutSessionRequest, opts ...grpc.CallOption) (*PutSessionResponse, error)
	// ClearSession drops a checkpoint once its procedure has finished.
	ClearSession(ctx context.Context, in *ClearSessionRequest, opts ...grpc.CallOption) (*ClearSessionResponse, error)
	// SetProfileKind records what model this unit is.
	SetProfileKind(ctx context.Context, in *SetProfileKindRequest, opts ...grpc.CallOption) (*SetProfileKindResponse, error)
	// SetStableId binds the unit to the device behind it, in the platform's
	// existing stable-id scheme (by-id:… survives a port move, by-path:… is
	// topology). Not a raw serial: a servo bus on /dev/ttyACM1 renumbers across
	// boots exactly as /dev/videoN does.
	SetStableId(ctx context.Context, in *SetStableIdRequest, opts ...grpc.CallOption) (*SetStableIdResponse, error)
	// StreamJointPositions reports where this robot's joints are, as fast as the
	// robot publishes them and for as long as the client listens.
	//
	// It is here, on the device, because DDS discovery is multicast: a robot's
	// ROS 2 graph lives on the robot's own network segment and a laptop cannot
	// see it across a routed link or a cloud tunnel, however reachable the device
	// is. The agent is already standing inside that segment — it joins the same
	// domain to read the battery — so the calibration wizard reads joints the way
	// it reads everything else, over this connection, and `wendy device robot
	// calibrate` works from a laptop like every other command.
	//
	// Streaming rather than polled because the wizard samples continuously while
	// an operator moves a joint by hand: the extremes of a sweep are what is
	// being measured, and a poll-per-sample would put a round trip between two
	// readings of a moving arm.
	//
	// Read-only by construction. There is no RPC here that could command a
	// joint, which is what keeps a nothing-powered procedure nothing-powered.
	//
	// Failures are separated by code, because "the robot is silent" and "we could
	// not listen" lead an operator to different places:
	//
	//	INVALID_ARGUMENT    a backend this agent cannot read, named with the ones
	//	                    it can.
	//	FAILED_PRECONDITION the device could not join a DDS domain at all — no
	//	                    eligible interface, or the sockets would not open.
	//	                    Nothing was heard because nothing was listening.
	//	NOT_FOUND           the device listened and nothing published the topic.
	//	                    Carries the ROBOT_JOINT_SOURCE_ABSENT reason so a
	//	                    client can tell this from a tunnel that lost the
	//	                    device, rather than matching on message text.
	//	INTERNAL            bytes arrived on the topic and the decoder refused
	//	                    them. The wire layout is not what this agent expects,
	//	                    which is reported rather than decoded into plausible
	//	                    wrong angles.
	StreamJointPositions(ctx context.Context, in *StreamJointPositionsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[JointPositionSample], error)
	// DescribeStore says where on the device the store physically is, for the
	// operator and for error messages. The client calls it once when it opens the
	// store, which doubles as the check that this agent serves the store at all:
	// an older agent answers UNIMPLEMENTED and the CLI can say so by name instead
	// of failing on the first write.
	DescribeStore(ctx context.Context, in *DescribeStoreRequest, opts ...grpc.CallOption) (*DescribeStoreResponse, error)
}

type wendyRobotServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewWendyRobotServiceClient(cc grpc.ClientConnInterface) WendyRobotServiceClient {
	return &wendyRobotServiceClient{cc}
}

func (c *wendyRobotServiceClient) LoadUnit(ctx context.Context, in *LoadUnitRequest, opts ...grpc.CallOption) (*LoadUnitResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(LoadUnitResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_LoadUnit_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) PutRecord(ctx context.Context, in *PutRecordRequest, opts ...grpc.CallOption) (*PutRecordResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(PutRecordResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_PutRecord_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) ClearRecord(ctx context.Context, in *ClearRecordRequest, opts ...grpc.CallOption) (*ClearRecordResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ClearRecordResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_ClearRecord_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) PutSession(ctx context.Context, in *PutSessionRequest, opts ...grpc.CallOption) (*PutSessionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(PutSessionResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_PutSession_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) ClearSession(ctx context.Context, in *ClearSessionRequest, opts ...grpc.CallOption) (*ClearSessionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ClearSessionResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_ClearSession_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) SetProfileKind(ctx context.Context, in *SetProfileKindRequest, opts ...grpc.CallOption) (*SetProfileKindResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SetProfileKindResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_SetProfileKind_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) SetStableId(ctx context.Context, in *SetStableIdRequest, opts ...grpc.CallOption) (*SetStableIdResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(SetStableIdResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_SetStableId_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *wendyRobotServiceClient) StreamJointPositions(ctx context.Context, in *StreamJointPositionsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[JointPositionSample], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &WendyRobotService_ServiceDesc.Streams[0], WendyRobotService_StreamJointPositions_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[StreamJointPositionsRequest, JointPositionSample]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WendyRobotService_StreamJointPositionsClient = grpc.ServerStreamingClient[JointPositionSample]

func (c *wendyRobotServiceClient) DescribeStore(ctx context.Context, in *DescribeStoreRequest, opts ...grpc.CallOption) (*DescribeStoreResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(DescribeStoreResponse)
	err := c.cc.Invoke(ctx, WendyRobotService_DescribeStore_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// WendyRobotServiceServer is the server API for WendyRobotService service.
// All implementations must embed UnimplementedWendyRobotServiceServer
// for forward compatibility.
//
// WendyRobotService reaches the device's robot calibration store from off the
// device.
//
// A calibration is a fact about the machine, so it lives on the machine — one
// JSON file per unit under /var/lib/wendy/robot, beside the camera registries
// the agent already owns. Without this service the calibration wizard can only
// run from inside an admin-entitled container on the robot itself, because the
// alternative — writing the record into the operator's home directory — turns a
// per-robot fact into a per-laptop one and the robot silently stops knowing its
// own calibration the moment somebody else connects to it.
//
// The surface is deliberately a transport for one Go interface,
// internal/shared/robotcal.Store, and nothing else: one RPC per method, no
// conveniences. The agent serves it from the same robotcal.FileStore the
// on-device path opens directly, so the two cannot drift apart — there is one
// implementation of the store's semantics, reached two ways.
//
// What the platform does NOT do here is interpret a calibration. `payload` and
// a session's per-step results are opaque bytes end to end: a rigid transform
// for a humanoid's camera, per-joint zeros and travel ranges for a 6-DOF arm,
// and something neither anticipates for the next robot. Only the universal part
// — did it pass, by how much, when, and by what method — is modelled.
type WendyRobotServiceServer interface {
	// LoadUnit returns everything one physical robot knows about itself. A unit
	// nobody has ever calibrated is not an error: it comes back zero-valued, with
	// its name filled in.
	LoadUnit(context.Context, *LoadUnitRequest) (*LoadUnitResponse, error)
	// PutRecord stores one calibration result, whatever its verdict. A failed
	// calibration is recorded too — "measured and it does not fit" is a fact
	// worth keeping, and it is not the same as never having run.
	PutRecord(context.Context, *PutRecordRequest) (*PutRecordResponse, error)
	// ClearRecord invalidates a calibration, and any half-finished attempt at it.
	// After a hand is replaced the old calibration is worse than none, because
	// everything downstream still trusts it. Clearing an id the unit does not
	// have is NOT_FOUND, so a typo cannot read as success.
	ClearRecord(context.Context, *ClearRecordRequest) (*ClearRecordResponse, error)
	// PutSession checkpoints an in-progress procedure. A seven-joint sweep on a
	// limp humanoid needs two people and a gantry; making them start over because
	// a terminal was closed at joint four is how a procedure stops being run.
	PutSession(context.Context, *PutSessionRequest) (*PutSessionResponse, error)
	// ClearSession drops a checkpoint once its procedure has finished.
	ClearSession(context.Context, *ClearSessionRequest) (*ClearSessionResponse, error)
	// SetProfileKind records what model this unit is.
	SetProfileKind(context.Context, *SetProfileKindRequest) (*SetProfileKindResponse, error)
	// SetStableId binds the unit to the device behind it, in the platform's
	// existing stable-id scheme (by-id:… survives a port move, by-path:… is
	// topology). Not a raw serial: a servo bus on /dev/ttyACM1 renumbers across
	// boots exactly as /dev/videoN does.
	SetStableId(context.Context, *SetStableIdRequest) (*SetStableIdResponse, error)
	// StreamJointPositions reports where this robot's joints are, as fast as the
	// robot publishes them and for as long as the client listens.
	//
	// It is here, on the device, because DDS discovery is multicast: a robot's
	// ROS 2 graph lives on the robot's own network segment and a laptop cannot
	// see it across a routed link or a cloud tunnel, however reachable the device
	// is. The agent is already standing inside that segment — it joins the same
	// domain to read the battery — so the calibration wizard reads joints the way
	// it reads everything else, over this connection, and `wendy device robot
	// calibrate` works from a laptop like every other command.
	//
	// Streaming rather than polled because the wizard samples continuously while
	// an operator moves a joint by hand: the extremes of a sweep are what is
	// being measured, and a poll-per-sample would put a round trip between two
	// readings of a moving arm.
	//
	// Read-only by construction. There is no RPC here that could command a
	// joint, which is what keeps a nothing-powered procedure nothing-powered.
	//
	// Failures are separated by code, because "the robot is silent" and "we could
	// not listen" lead an operator to different places:
	//
	//	INVALID_ARGUMENT    a backend this agent cannot read, named with the ones
	//	                    it can.
	//	FAILED_PRECONDITION the device could not join a DDS domain at all — no
	//	                    eligible interface, or the sockets would not open.
	//	                    Nothing was heard because nothing was listening.
	//	NOT_FOUND           the device listened and nothing published the topic.
	//	                    Carries the ROBOT_JOINT_SOURCE_ABSENT reason so a
	//	                    client can tell this from a tunnel that lost the
	//	                    device, rather than matching on message text.
	//	INTERNAL            bytes arrived on the topic and the decoder refused
	//	                    them. The wire layout is not what this agent expects,
	//	                    which is reported rather than decoded into plausible
	//	                    wrong angles.
	StreamJointPositions(*StreamJointPositionsRequest, grpc.ServerStreamingServer[JointPositionSample]) error
	// DescribeStore says where on the device the store physically is, for the
	// operator and for error messages. The client calls it once when it opens the
	// store, which doubles as the check that this agent serves the store at all:
	// an older agent answers UNIMPLEMENTED and the CLI can say so by name instead
	// of failing on the first write.
	DescribeStore(context.Context, *DescribeStoreRequest) (*DescribeStoreResponse, error)
	mustEmbedUnimplementedWendyRobotServiceServer()
}

// UnimplementedWendyRobotServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedWendyRobotServiceServer struct{}

func (UnimplementedWendyRobotServiceServer) LoadUnit(context.Context, *LoadUnitRequest) (*LoadUnitResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method LoadUnit not implemented")
}
func (UnimplementedWendyRobotServiceServer) PutRecord(context.Context, *PutRecordRequest) (*PutRecordResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method PutRecord not implemented")
}
func (UnimplementedWendyRobotServiceServer) ClearRecord(context.Context, *ClearRecordRequest) (*ClearRecordResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ClearRecord not implemented")
}
func (UnimplementedWendyRobotServiceServer) PutSession(context.Context, *PutSessionRequest) (*PutSessionResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method PutSession not implemented")
}
func (UnimplementedWendyRobotServiceServer) ClearSession(context.Context, *ClearSessionRequest) (*ClearSessionResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method ClearSession not implemented")
}
func (UnimplementedWendyRobotServiceServer) SetProfileKind(context.Context, *SetProfileKindRequest) (*SetProfileKindResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method SetProfileKind not implemented")
}
func (UnimplementedWendyRobotServiceServer) SetStableId(context.Context, *SetStableIdRequest) (*SetStableIdResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method SetStableId not implemented")
}
func (UnimplementedWendyRobotServiceServer) StreamJointPositions(*StreamJointPositionsRequest, grpc.ServerStreamingServer[JointPositionSample]) error {
	return status.Error(codes.Unimplemented, "method StreamJointPositions not implemented")
}
func (UnimplementedWendyRobotServiceServer) DescribeStore(context.Context, *DescribeStoreRequest) (*DescribeStoreResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method DescribeStore not implemented")
}
func (UnimplementedWendyRobotServiceServer) mustEmbedUnimplementedWendyRobotServiceServer() {}
func (UnimplementedWendyRobotServiceServer) testEmbeddedByValue()                           {}

// UnsafeWendyRobotServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to WendyRobotServiceServer will
// result in compilation errors.
type UnsafeWendyRobotServiceServer interface {
	mustEmbedUnimplementedWendyRobotServiceServer()
}

func RegisterWendyRobotServiceServer(s grpc.ServiceRegistrar, srv WendyRobotServiceServer) {
	// If the following call panics, it indicates UnimplementedWendyRobotServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&WendyRobotService_ServiceDesc, srv)
}

func _WendyRobotService_LoadUnit_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(LoadUnitRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).LoadUnit(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_LoadUnit_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).LoadUnit(ctx, req.(*LoadUnitRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_PutRecord_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(PutRecordRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).PutRecord(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_PutRecord_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).PutRecord(ctx, req.(*PutRecordRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_ClearRecord_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ClearRecordRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).ClearRecord(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_ClearRecord_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).ClearRecord(ctx, req.(*ClearRecordRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_PutSession_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(PutSessionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).PutSession(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_PutSession_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).PutSession(ctx, req.(*PutSessionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_ClearSession_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ClearSessionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).ClearSession(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_ClearSession_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).ClearSession(ctx, req.(*ClearSessionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_SetProfileKind_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SetProfileKindRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).SetProfileKind(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_SetProfileKind_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).SetProfileKind(ctx, req.(*SetProfileKindRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_SetStableId_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(SetStableIdRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).SetStableId(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_SetStableId_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).SetStableId(ctx, req.(*SetStableIdRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WendyRobotService_StreamJointPositions_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(StreamJointPositionsRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(WendyRobotServiceServer).StreamJointPositions(m, &grpc.GenericServerStream[StreamJointPositionsRequest, JointPositionSample]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WendyRobotService_StreamJointPositionsServer = grpc.ServerStreamingServer[JointPositionSample]

func _WendyRobotService_DescribeStore_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(DescribeStoreRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WendyRobotServiceServer).DescribeStore(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WendyRobotService_DescribeStore_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WendyRobotServiceServer).DescribeStore(ctx, req.(*DescribeStoreRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// WendyRobotService_ServiceDesc is the grpc.ServiceDesc for WendyRobotService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var WendyRobotService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "wendy.agent.services.v2.WendyRobotService",
	HandlerType: (*WendyRobotServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "LoadUnit",
			Handler:    _WendyRobotService_LoadUnit_Handler,
		},
		{
			MethodName: "PutRecord",
			Handler:    _WendyRobotService_PutRecord_Handler,
		},
		{
			MethodName: "ClearRecord",
			Handler:    _WendyRobotService_ClearRecord_Handler,
		},
		{
			MethodName: "PutSession",
			Handler:    _WendyRobotService_PutSession_Handler,
		},
		{
			MethodName: "ClearSession",
			Handler:    _WendyRobotService_ClearSession_Handler,
		},
		{
			MethodName: "SetProfileKind",
			Handler:    _WendyRobotService_SetProfileKind_Handler,
		},
		{
			MethodName: "SetStableId",
			Handler:    _WendyRobotService_SetStableId_Handler,
		},
		{
			MethodName: "DescribeStore",
			Handler:    _WendyRobotService_DescribeStore_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "StreamJointPositions",
			Handler:       _WendyRobotService_StreamJointPositions_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "wendy/agent/services/v2/robot_service.proto",
}
